# Shade Sanctuary — Fencing & Gates (Adelaide)

Static marketing site. Plain HTML, CSS and JavaScript with **no build step**.
The root of this repository is the deployable site.

## Deploying on Vercel
- Framework Preset: Other
- Build Command: (leave empty)
- Output Directory: (leave default)
- Install Command: (leave empty)

`index.html` is the home page; `404.html` is the not-found page.
